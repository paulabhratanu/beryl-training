git --version
git config --global user.name "abc
git config --global user.email "abc@gmail.com"
git help config
git init ->initialize an empty repo(.git)
rm -rf git ->remove(.git)
git status ->list the files
touch .gitignore ->create a gitignore file and add the hidden files
git add -A  ->add everything into the staging area
git reset   ->remove everything from the staging area
git commit -m "initial commit"
git log ->details about the commit
git <clone> <where to clone> ->cloning a repo OR git clone [url]
git diff ->changes in the repo
git remote add origin https://github.com/paulabhratanu/df.git
git push -u origin
git pull origin master
git rm --cached file1.txt
git rm -r --cached FolderName
git branch <branch name> ->create another branch
git checkout tree
git branch -d <branch name>   ->delete the branch

#steps
git init
git add README.md
git commit -m "first commit"
git remote add origin1 https://github.com/paulabhratanu/Majot-project-2017.git
git push -u origin1 master

#rejected error

git fetch origin master:tmp
git rebase tmp
git push origin HEAD:master
